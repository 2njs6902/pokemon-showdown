import { FormatData } from '../../../sim/dex-formats';

export const Formats: FormatData[] = [
	// {
	// 	name: "Rejuvenation",
	// 	mod: 'rejuvenation',
	// 	desc: `Unofficial Rejuvenation Mod for Pokemon Showdown`,
	// 	ruleset: ['Standard NatDex'],
	// 	// teambuilderFormat: 'National Dex', // (uncomment this line if your mod is natdex)
	// 	onValidateTeam(team, format) {
	// 		let speciesTable = {};
	// 		let allowedTiers = ['LC', 'NFE', 'OU', 'UU'];
	// 		for (const set of team) {
	// 			let template = this.dex.species.get(set.species);
	// 			if (!allowedTiers.includes(template.tier)) {
	// 				return [set.species + ' is not legal in Rejuvenation Mod.'];
	// 			}
	// 		}
	// 	},
	// }
];